/**
 * course 域：商业课程实体。
 *
 * <p>MVP 范围：录播课程（章节/小节/视频）、学习进度、自动续播、仅在线观看（防盗链/播放凭证）。
 * 直播在 MVP 明确不实现，仅预留 course 类型扩展点（type 字段设计时预留）。</p>
 *
 * <p>铁律：课程 != 考试科目。课程通过关联关系挂接到 subject 域的知识点/章节。</p>
 *
 * <p>模块边界规则同 sys 域。</p>
 */
package com.yunsie.module.course;
