/**
 * exam 域：模拟考试引擎。
 *
 * <p>MVP 范围：规则组卷（可配）、计时（服务端时间为准）、自动保存、自动交卷（幂等）、
 * 判分、成绩、解析、答题卡、考试记录。</p>
 *
 * <p>硬性要求：试卷实例快照题目（生成后不可变）；交卷并发用条件更新；成绩落库触发学习档案事件（user 域）。</p>
 *
 * <p>正式考试模式（监考/防作弊）MVP 明确不实现。</p>
 *
 * <p>模块边界规则同 sys 域。</p>
 */
package com.yunsie.module.exam;
