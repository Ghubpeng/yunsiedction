<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.yunsie</groupId>
        <artifactId>yunsie-server</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>yunsie-module-exam</artifactId>
    <name>yunsie-module-exam</name>
    <description>exam 域：模拟考试（组卷/计时/自动保存/自动交卷/判分/成绩/解析/答题卡/考试记录）。试卷快照不可变，交卷幂等。依赖 user / question / subject。</description>

    <dependencies>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-server-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-user</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-question</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-subject</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 证书存在性校验（CertificateQueryApi 契约） -->
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-certificate</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 领域基础设施：Web(Controller) / ORM(Mapper) / 校验 / @PreAuthorize / Lombok -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- 单元测试 Bean Validation 提供者（DTO 校验测试） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
