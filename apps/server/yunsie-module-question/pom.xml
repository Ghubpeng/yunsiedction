<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.yunsie</groupId>
        <artifactId>yunsie-server</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>yunsie-module-question</artifactId>
    <name>yunsie-module-question</name>
    <description>question 域：题库（单选/多选/判断，题型可扩展）、错题本、审核流、版本与快照、Excel 导入。依赖 user / subject / certificate。</description>

    <dependencies>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-server-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-user</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-subject</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-certificate</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 章节练习：经 course 域 api 包读取章节→知识点节点关联（新边 question→course，无环） -->
        <dependency>
            <groupId>com.yunsie</groupId>
            <artifactId>yunsie-module-course</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 领域基础设施：Web(Controller) / ORM(Mapper) / 校验 / @PreAuthorize / Excel 导入 / Lombok -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
