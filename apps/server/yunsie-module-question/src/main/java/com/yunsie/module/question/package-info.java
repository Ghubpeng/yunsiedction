/**
 * question 域：题库与错题。
 *
 * <p>MVP 范围：单选/多选/判断（题型字典可扩展）、Excel 导入（行级校验、全部通过才入库）、
 * 审核流（草稿→待审→通过→下架）、知识点关联、版本化（改题=新版本）、考试引用快照。</p>
 *
 * <p>题目是考试（exam）与知识库（RAG）的共同上游；每题必须有解析（AI 答疑与错题讲解依赖）。</p>
 *
 * <p>模块边界规则同 sys 域。</p>
 */
package com.yunsie.module.question;
