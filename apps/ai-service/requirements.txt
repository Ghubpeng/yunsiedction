# yunsie-ai-service —— AI 服务骨架依赖
# Stage 1 仅骨架；Provider 具体实现（OpenAI 兼容协议，httpx 直连）在 AI 场景开发阶段完成。
# 版本采用保守下限；正式开发时锁定精确版本。
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic-settings>=2.4,<3.0
httpx>=0.27,<1.0
